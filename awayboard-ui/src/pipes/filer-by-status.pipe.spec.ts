import { FilerByStatusPipe } from './filer-by-status.pipe';

describe('FilerByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
